# ────────────────────────────────────────────────────────────────────
# Dependências Python (pip)
# ────────────────────────────────────────────────────────────────────
# Pra deps de SISTEMA (Chrome, ffmpeg) ver: install_system_deps.sh
# ────────────────────────────────────────────────────────────────────

feedparser
google-genai
google-api-python-client
google-auth-oauthlib
edge-tts
beautifulsoup4
requests
python-dotenv
aiofiles
python-telegram-bot>=20.0
Pillow
moviepy<2.0
numpy
scipy
groq
instagrapi

# TikTok (precisa do Google Chrome instalado no sistema — ver install_system_deps.sh)
tiktok-uploader

# NotebookLM (legado — só usado em notebooklm_session.check() que não é chamado hoje,
# mas import ainda existe; mantido pra não quebrar)
notebooklm-py
